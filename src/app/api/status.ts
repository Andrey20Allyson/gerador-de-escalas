export enum SaveWorkersDaysOfWorkStatus {
  OK,
  DATA_NOT_LOADED_ERROR,
  ARRAY_LENGTH_ERROR,
  UNKNOWN_ERROR,
};

export enum GeneratorStatus {
  OK,
  ASSIGN_ERROR,
  DATA_NOT_LOADED_ERROR,
  UNKNOW_ERROR,
}